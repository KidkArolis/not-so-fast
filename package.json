{
  "name": "not-so-fast",
  "description": "Fast and efficient in-memory rate-limit for Node, used to alleviate DOS attacks.",
  "version": "0.0.0",
  "homepage": "https://github.com/KidkArolis/not-so-fast",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/KidkArolis/not-so-fast.git"
  },
  "bugs": {
    "url": "https://github.com/KidkArolis/not-so-fast/issues"
  },
  "licenses": [
    {
      "type": "MIT",
      "url": "https://github.com/KidkArolis/not-so-fast/blob/master/LICENSE"
    }
  ],
  "scripts": {
    "test": "npm run lint && istanbul cover _mocha",
    "coverage": "istanbul cover _mocha --report html",
    "lint": "healthier && prettier --check '**/*.{js,json,css}'",
    "format": "prettier --write '**/*.{js,json,css,yml}'"
  },
  "healthier": {
    "globals": [
      "describe",
      "it"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "healthier": "^2.0.0",
    "istanbul": "0.4.5",
    "mocha": "3.4.2",
    "mocha-lcov-reporter": "1.3.0",
    "np": "^5.0.3",
    "prettier": "^1.18.2"
  },
  "keywords": [
    "ratelimit",
    "rate-limit",
    "rate",
    "limit",
    "attack",
    "flood",
    "security",
    "dos",
    "ddos"
  ]
}
